{
  "version": 2,
  "builds": [
    { "src": "api/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/notes/(.*)", "dest": "/api/notes.js?id=$1" },
    { "src": "/api/notes",      "dest": "/api/notes.js" }
  ]
}
